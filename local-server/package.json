{
  "name": "local-observability-server",
  "version": "1.0.0",
  "description": "Local development server for log forwarding and observability",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "bun-types": "latest",
    "concurrently": "^9.2.0"
  }
}